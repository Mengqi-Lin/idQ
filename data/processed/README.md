# Processed data

`python -m idQ.experiments.summary` writes the numeric row-sparsity summary and
its percentage-formatted display copy here. Keep raw per-seed CSVs in `../raw/`.
Use a separate output path when summarizing a selected run or experiment subset.
